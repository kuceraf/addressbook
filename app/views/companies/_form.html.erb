<%= form_for(@company) do |company_form| %>
  <% if @company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@company.errors.count, "error") %> prohibited this company from being saved:</h2>

      <ul>
      <% @company.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="color new_edit">
  <h3>Name</h3>
    <%= company_form.label :name, 'Name:' %><br />
    <%= company_form.text_field :name %>
  </div>
  
  <div class="field new_edit color2">
  <h3>Email</h3>
  <%= render :partial => 'emails/form',
              :locals => {:form => company_form}%>
  </div>
              
  <div class="color new_edit">
  <h3>Address</h3>
  <%= render :partial => 'addresses/form',
              :locals => {:form => company_form}%>
  </div>
  
  <div class="new_edit color2">
  <h3>Phone</h3>
  <%= render :partial => 'phones/form',
              :locals => {:form => company_form}%>
  </div>
  <div class="actions">
    <%= company_form.submit %>
  </div>
<% end %>

